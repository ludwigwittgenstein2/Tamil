# _brand.yml

defaults:
  bootstrap:
    defaults:
      # make the navbar dark navy
      navbar-bg: "#2C3E50"
      # make all navbar text/icons white
      navbar-fg: "#FFFFFF"
      # highlight color on hover or active link
      navbar-hl: "#1ABC9C"
